package com.techlabs.component.model;

public interface Shape {

	public void draw(String fillColor);
	public double area();
	
	// Base Component interface or abstract all objects in the composition, client program uses base component to work with the objects in the composition.
}
